This directory contains archived build artifacts and caches moved out of the repository
to keep the repo lightweight for contributors.

Files moved here on 2026-07-16:
- api_apps_api_binary  (binary moved from apps/api/api)
- api_root_binary     (binary moved from repo root `api`)
- web_dot_next        (Next.js build cache moved from apps/web/.next)

If you need to restore any artifact, move it back to the original path and adjust
executable bits if necessary.

Consider removing these files permanently or storing them in release assets instead.
